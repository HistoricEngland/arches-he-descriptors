
services:
    nginx-archeshedescriptors:
      container_name: nginx-archeshedescriptors
      image: nginx:latest
      ports:
        - "8080:80"
      volumes:
        - ./docker/nginx-config/default.conf:/etc/nginx/conf.d/default.conf
      networks:
        - archeshedescriptors-network

    db-archeshedescriptors:
      container_name: db-archeshedescriptors
      image: postgis/postgis:14-3.2
      volumes:
        - postgres-data:/var/lib/postgresql/data
        - postgres-log:/var/log/postgresql
        - ./docker/init-unix.sql:/docker-entrypoint-initdb.d/init.sql # to set up the DB template
      ports:
        - '5433:5432'
      env_file: 
        - ./docker/env_file.env
      networks:
        - archeshedescriptors-network

    elasticsearch-archeshedescriptors:
      container_name: elasticsearch-archeshedescriptors
      image: docker.elastic.co/elasticsearch/elasticsearch:8.10.4
      volumes:
        #- ../../elasticsearch_data_arches_he_descriptors:/usr/share/elasticsearch/data
        - elasticsearch-data:/usr/share/elasticsearch/data
      ports:
        - "9201:9200"
        - "9301:9300"
      env_file: 
        - ./docker/env_file.env
      networks:
        - archeshedescriptors-network

    rabbitmq-archeshedescriptors:
      container_name: rabbitmq-archeshedescriptors
      image: rabbitmq:3.12.6-management
      hostname: my-rabbit
      volumes:
        - rabbitmq-data:/var/lib/rabbitmq/mnesia/rabbit@my-rabbit
        - rabbitmq-logs:/var/log/rabbitmq/log
      ports:
        - 5673:5672
        - 15673:15672
      networks:
        - archeshedescriptors-network

    #pg-feat-archeshedescriptors:
    #  container_name: pg-feat-archeshedescriptors
    #  image: pramsey/pg_featureserv
    #  volumes:
    #    - ../../pg_featureserv_config_arches_he_descriptors:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-archeshedescriptors:5432/arches_he_descriptors
    #  ports:
    #    - 9000:9000
    #  depends_on:
    #    - db-archeshedescriptors
    #  networks:
    #    - archeshedescriptors-network
    
    #pg-tile-archeshedescriptors:
    #  container_name: pg-tile-archeshedescriptors
    #  image: pramsey/pg_tileserv
    #  volumes:
    #    - ../../pg_tileserv_config_arches_he_descriptors:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-archeshedescriptors:5432/arches_he_descriptors
    #  ports:
    #    - 7800:7800
    #  depends_on:
    #    - db-archeshedescriptors
    #  networks:
    #    - archeshedescriptors-network


networks:
  archeshedescriptors-network:
    name: archeshedescriptors-network
    driver: bridge

volumes:
    postgres-data:
    postgres-log:
    elasticsearch-data:
    rabbitmq-logs:
    rabbitmq-data:
